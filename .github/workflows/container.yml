name: Publish container

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      version:
        description: Existing tagged version to publish, without the v prefix
        required: true
        type: string
      update_floating_tags:
        description: Update minor and latest; only for the newest stable X.Y.Z version
        required: true
        default: false
        type: boolean

concurrency:
  group: container-publish
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'release' && github.event.release.tag_name || format('v{0}', inputs.version) }}
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: false
      - name: Verify the release version
        id: release
        env:
          RELEASE_TAG: ${{ github.event.release.tag_name }}
          RELEASE_PRERELEASE: ${{ github.event.release.prerelease }}
          REQUESTED_VERSION: ${{ inputs.version }}
          UPDATE_FLOATING_TAGS: ${{ inputs.update_floating_tags }}
        run: |
          set -euo pipefail
          PACKAGE_VERSION="$(uv version --short)"
          if [[ "${GITHUB_EVENT_NAME}" == "release" ]]; then
            EXPECTED_TAG="${RELEASE_TAG}"
          else
            EXPECTED_TAG="v${REQUESTED_VERSION}"
          fi
          if [[ "${EXPECTED_TAG}" != "v${PACKAGE_VERSION}" ]]; then
            echo "Expected ${EXPECTED_TAG} to match package version v${PACKAGE_VERSION}" >&2
            exit 1
          fi
          CHECKED_OUT_SHA="$(git rev-parse HEAD)"
          if [[ "$(git rev-list -n 1 "${EXPECTED_TAG}")" != "${CHECKED_OUT_SHA}" ]]; then
            echo "${EXPECTED_TAG} does not point at checked-out commit ${CHECKED_OUT_SHA}" >&2
            exit 1
          fi
          PUBLISH_FLOATING_TAGS=false
          REQUEST_FLOATING_TAGS=false
          if [[ "${GITHUB_EVENT_NAME}" == "release" ]]; then
            if [[ "${RELEASE_PRERELEASE}" == "false" ]]; then
              REQUEST_FLOATING_TAGS=true
            fi
          elif [[ "${UPDATE_FLOATING_TAGS}" == "true" ]]; then
            REQUEST_FLOATING_TAGS=true
          fi
          if [[ "${REQUEST_FLOATING_TAGS}" == "true" ]]; then
            if [[ ! "${PACKAGE_VERSION}" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
              echo "floating tags require a stable X.Y.Z package version" >&2
              exit 1
            fi
            LATEST_STABLE_TAG=""
            while IFS= read -r CANDIDATE_TAG; do
              if [[ "${CANDIDATE_TAG}" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
                LATEST_STABLE_TAG="${CANDIDATE_TAG}"
                break
              fi
            done < <(git tag --list "v*" --sort=-version:refname)
            if [[ "${EXPECTED_TAG}" != "${LATEST_STABLE_TAG}" ]]; then
              echo "Floating tags require newest stable ${LATEST_STABLE_TAG}, got ${EXPECTED_TAG}" >&2
              exit 1
            fi
            PUBLISH_FLOATING_TAGS=true
          fi
          echo "tag=${EXPECTED_TAG}" >> "${GITHUB_OUTPUT}"
          echo "publish_floating_tags=${PUBLISH_FLOATING_TAGS}" >> "${GITHUB_OUTPUT}"
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - id: metadata
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/neapolitanicecream/recoleta
          flavor: latest=false
          tags: |
            type=semver,pattern={{version}},value=${{ steps.release.outputs.tag }}
            type=semver,pattern={{major}}.{{minor}},value=${{ steps.release.outputs.tag }},enable=${{ steps.release.outputs.publish_floating_tags == 'true' }}
            type=raw,value=latest,enable=${{ steps.release.outputs.publish_floating_tags == 'true' }}
      - uses: docker/build-push-action@v6
        with:
          context: .
          target: runtime
          platforms: |
            linux/amd64
            linux/arm64
          push: true
          tags: ${{ steps.metadata.outputs.tags }}
          labels: ${{ steps.metadata.outputs.labels }}
          provenance: true
          sbom: true
